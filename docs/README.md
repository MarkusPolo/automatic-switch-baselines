# Documentation

This directory contains architectural details, schemas, and API documentation.

## Architecture
See [README.md](../README.md) for the 2-phase design.

## CSV Schema
The import CSV should follow this format:
`port, hostname, mgmt_ip, mask, gateway, mgmt_vlan, model`

## API Documentation
Once the server is running, visit `/docs` for the Swagger UI.
